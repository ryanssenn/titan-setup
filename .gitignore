# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
build/
dist/

# OS / editors
.DS_Store
*.swp
*~
.idea/
.vscode/

# Large / transient training outputs
outputs/*/checkpoint/
outputs/*/*.distcp
outputs/*/comm_trace/

# Keep only the curated small result files under outputs/
# (individual log exceptions are hard in .gitignore; we will be selective in git add)

# TorchTitan (large pinned checkout with its own history; treat separately or as submodule)
torchtitan/

# HF / cache
.cache/
huggingface/
